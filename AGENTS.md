<!-- project-memory:start -->
## Project memory
- At the start of each task, read `PROJECT_MEMORY.md` if it exists.
- Treat it as a compact handoff, not unquestionable truth; verify against the repository when needed.
- After substantial work, update it only when project state, decisions, invariants, blockers, or next actions materially changed.
- Keep it concise; replace stale facts instead of appending history.
<!-- project-memory:end -->
